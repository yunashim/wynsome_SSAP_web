


const timeButton = document.querySelectorAll('timeBox')
const timeText = document.querySelectorAll('timeText')

timeButton.addEventListener('click', function show() {
    timeButton.style.display = "flex"
})